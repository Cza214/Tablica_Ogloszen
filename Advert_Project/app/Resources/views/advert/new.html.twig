{% extends 'base.html.twig' %}

{% block content %}
    <h1>Advert creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
            <a href="{{ path('advert_index') }}" class="btn btn-secondary">Back to the list</a>
{% endblock %}
